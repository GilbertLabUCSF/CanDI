version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-4.10"

conda:
  environment: docs/environment.yaml

# run a bash command before building the docs
ansible.builtin.command:
  args: candi-install
